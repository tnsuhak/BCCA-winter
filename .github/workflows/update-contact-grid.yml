name: Update BCCA contact grid

on:
  push:
    branches:
      - fix/2027-bcca-schooling
    paths:
      - '.github/workflows/update-contact-grid.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/2027-bcca-schooling
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Update contact cards
        run: python tools/update_contact_grid.py
      - name: Commit contact grid update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet && exit 0
          git commit -m "Update consultation cards to four options [skip ci]"
          git push origin HEAD:fix/2027-bcca-schooling
